---
import MasterLayout from "./master.astro";

// type Props = {
//   frontmatter: {
//     title: string;
//   };
// };

const { frontmatter, body } = Astro.props;

if (!body && !frontmatter) {
  throw new Error("Post body and frontmatter must be defined");
}
---

<MasterLayout {...frontmatter}>
  <slot />
</MasterLayout>
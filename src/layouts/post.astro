---
import MasterLayout from "./master.astro";

// type Props = {
//   frontmatter: {
//     title: string;
//   };
// };

const { frontmatter, body } = Astro.props;

if (!frontmatter) {
  throw new Error("Frontmatter must be defined");
}
---

<MasterLayout {...frontmatter}>
  <slot />
</MasterLayout>
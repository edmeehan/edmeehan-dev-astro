---
import MarkdownIt from 'markdown-it';
import Card from "src/components/cards/post.astro";
const { content, cards, section } = Astro.props;
const md = new MarkdownIt({ html: true });
---
<style lang="scss" is:global>
  [data-cards-posts] {
    @extend %primary-wrapper;

    > header {
      text-align: center;
      padding-top: 4.5rem;
      padding-bottom: 3rem;
    }
    .rt > * {
      margin-left: auto;
      margin-right: auto;
    }
    > ul {
      margin: 0;
      padding-left: 0;
      padding-bottom: 4.5rem;
      list-style: none;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2em;

      // @include media-tablet {
      //   column-gap: 5%;
      //   row-gap: 2em;
      // }

      > li {
        @include media-tablet {
          flex: 0 0 30%;
        }
      }
    }
  }
</style>

<div data-cards-posts>
  <header>
    { content && <div class="rt" set:html={md.render(content)}></div>}
  </header>
  { cards && (
    <ul>
      { cards.map((item) => (
        <li>
          <Card show_excerpt={true} blog_uuid={item?.blog} project_uuid={item?.project}/>
        </li>
      ))}
    </ul>
  )}
</div>